/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` at build time from
// `git describe --tags --always --dirty` — see Footer.vue for where
// it's displayed.
declare const __APP_VERSION__: string
